;;; config/projectile.el -*- lexical-binding: t; -*-

;; TODO: ripgrep has options to exclude certain directories, or to use -t to
;; specify a certain language. However, -tclojure is long and would prefer
;; configuration to recognize -tclj instead. Set that up.
